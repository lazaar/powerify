{% layout none %}{
    {% if shop.metafields.powerify.settings != blank  %}
      "settings": {{ shop.metafields.powerify.settings | json}},
    {% else %}
        "settings": {},
    {% endif %}

  "moneyFormat": "{{ shop.money_format }}",
  "currency": "{{ shop.currency }}"

}